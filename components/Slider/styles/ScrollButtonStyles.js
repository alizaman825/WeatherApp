import { StyleSheet } from "react-native";

const scrollButtonStyles = StyleSheet.create({
    button: {
        position: "absolute",
        backgroundColor: "rgba(72, 75, 91, 1)",
        borderRadius: 100,
        padding: 10,
    },
});

export default scrollButtonStyles;
